# Core (already in requirements.txt)
numpy>=1.24.0

# Live trading connectors
MetaTrader5>=5.0.45        # MT5 Python API (Windows only)
fastapi>=0.110.0           # TradingView webhook server
uvicorn[standard]>=0.29.0  # ASGI server for FastAPI
httpx>=0.27.0              # Async HTTP client (for testing webhooks)
